
{
  "name": "evidence_analysis",
  "description": "Analyze which infoTypes have evidence and summarize evidence.",
  "parameters": {
    "type": "object",
    "required": ["infoTypes_with_evidence", "infoTypes_missing", "evidence_summary"],
    "properties": {
      "infoTypes_with_evidence": { "type": "array", "items": { "type": "string" } },
      "infoTypes_missing": { "type": "array", "items": { "type": "string" } },
      "evidence_summary": { "type": "array", "items": { "type": "string" } }
    }
  }
}
